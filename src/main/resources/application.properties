spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.url=jdbc:sqlite:D:/temp/us-census.db

app.config.table-name=census_learn_sql
app.config.column-to-average=age
# order by count or average
app.config.order-by=count
app.config.nb-entries=100

